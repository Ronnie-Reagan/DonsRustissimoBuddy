# Introduction

This app is pretty simple; You launch it once and it creates a `config.json` that you can edit to set the port, ip and output device. aswell as max upload size.

The buttons are broken in the center for pause and play; for now you must just press play on the song in the list.
